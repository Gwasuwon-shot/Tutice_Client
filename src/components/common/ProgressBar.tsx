import {styled} from 'styled-components';

export default function ProgressBar() {
    return (
        <ProgressBarWrapper>
            
        </ProgressBarWrapper>
    )
}

const ProgressBarWrapper = styled.header`
`
